#include "DataTypes.cpp"

class English : public SUBJECT_t {
    public:
    English() = delete;
    // Need to add constructor
};

// these classes will have their own static max_marks, and local marks of each student in that subject